# GOAL: 统查 send_to 项目 Bug 并修复，评估代码可读性

## 开始时间
2026-06-18 02:48

## 审查范围
send_to 插件全部 14 个源文件 + 1 个测试文件

## 审查方法
逐文件通读 + 交叉检查 + 模式一致性分析

## 进展记录

### 02:50 - 审查完成，发现问题
- 发现 5 个确认 Bug
- 发现 2 个代码一致性问题

### 02:54 - 修复计划制定
- 创建了修复计划 `.agents/context/20260618_025424_send_to_插件_Bug_修复与代码可读性改进.md`
- GOAL 模式下无需用户批准，直接进入执行

### Bug 清单
1. **B1 (高)** stream_index.py `_get_config` 缺少 getattr 保护 → 可能崩溃
2. **B2 (高)** daily_memory.py `_get_config` 同问题
3. **B3 (中)** tools.py SendToFindStreamTool 索引歧义：<=4位数字强制当作索引
4. **B4 (中)** tools.py SendToStreamContextTool 同上
5. **B5 (中)** tools.py SendToDailyMemoryTool 同上

### 代码一致性问题
- C1: 4个 `_get_config`/`get_config` 实现不一致
- C2: 2个文件用 CRLF，其余用 LF

## 注意事项
- stream_index 和 daily_memory 的 `_get_config` 直接用 `plugin.config` 而非 `getattr(plugin, "config", None)`，在 plugin 未完全初始化时会 AttributeError
- tools.py 中 3 个 Tool 将 1-4 位数字当作列表索引，但 4 位数字可能是 QQ 群号
- 全部改为安全访问模式，阈值从 4 降到 3
